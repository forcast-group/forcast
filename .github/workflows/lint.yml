name: Lint Check
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    steps:
      - uses: ./common-step.yml

      - name: Run lint
        run: npm run lint
